#include "hello.h" 
// if write this, the compiler will first search the hello.h file in the current directory, so you should add the include directory to the environment variable. 

int hello(){
    return 1;
}
